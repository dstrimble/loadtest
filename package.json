{
  "name": "loadtesting",
  "version": "1.0.0",
  "description": "loadtesting",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Trimble",
  "dependencies": {
    "restify": "^4.3.0"
  }
}
